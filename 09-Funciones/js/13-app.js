// Arrow functions

const reproductor = {
    reproducir: (id) => {
        console.log(`Reproduciendo canción con el ID ${id}`)
    },
    pausar: () => {
        console.log('Pausando...')
    }
}

reproductor.reproducir(30);

