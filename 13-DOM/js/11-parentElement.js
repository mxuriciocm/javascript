const card = document.querySelector('.card');

console.log(card.parentNode) // Muestra el elemento padre y los espacios en blanco del HTML
console.log(card.parentElement) // Muestra solo el elemento padre
console.log(card.parentElement.parentElement) // Mostrar padre del padre