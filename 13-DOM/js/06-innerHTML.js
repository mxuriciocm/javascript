const header = document.querySelector('.contenido-hero h1');
console.log(header);

// Trae el HTML
console.log(header.innerHTML);

const header1 = document.querySelector('.contenido-hero h1').innerHTML;