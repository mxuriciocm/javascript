const card = document.querySelector('.card');
// Agregar clases para luego modificar los estilos en CSS
card.classList.add('cambiar-color');

// Eliminar clases
card.classList.remove('cambiar-color');